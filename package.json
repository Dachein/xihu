{
  "name": "xihu",
  "version": "0.1.0",
  "private": true,
  "description": "Worker-hosted mobile-first cultural terrain map for Hangzhou West Lake.",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "deploy:pages": "npx wrangler pages deploy public --project-name xihu",
    "check": "node --check worker/index.js && node --check public/src/main.js"
  }
}
